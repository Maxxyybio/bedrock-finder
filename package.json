{
  "name": "bedrock-finder",
  "version": "1.0.0",
  "description": "Minecraft coordinate finder using bedrock pattern recognition",
  "private": true,
  "workspaces": [
    "shared",
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w @bedrock-finder/backend\" \"npm run dev -w @bedrock-finder/frontend\"",
    "build": "npm run build -w @bedrock-finder/shared && npm run build -w @bedrock-finder/backend && npm run build -w @bedrock-finder/frontend",
    "test": "npm run test -w @bedrock-finder/backend",
    "lint": "eslint . --ext .ts,.tsx --ignore-pattern node_modules",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "concurrently": "^8.2.0",
    "eslint": "^8.50.0",
    "prettier": "^3.0.3",
    "typescript": "^5.2.2"
  }
}